<li>
  <%= gravatar_for user, size: Settings.user.image.link %>
  <%= link_to user.name, user %>
  <% if can_destroy_user? user %>
    | <%= link_to t(".delete"), user, data: { turbo_method: :delete, turbo_confirm: t(".confirm") } %>
  <% end %>
</li>
